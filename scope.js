For machine test question

Create a collection in monogodb to
 store user information(ID,Name,email,phone,password, profile image)
  and create two api's in node js to insert and update user info. Add 
  Validation in API so that user cannot upload large image,
   MAX 2MB allowed.